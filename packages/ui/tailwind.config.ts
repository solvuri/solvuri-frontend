import type { Config } from "tailwindcss";

// Color tokens live in globals.css `@theme` blocks (see src/styles/solvuri-theme.css), not here.
const config: Config = {
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
